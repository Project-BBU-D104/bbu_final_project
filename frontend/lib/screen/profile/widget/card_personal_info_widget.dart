import 'package:flutter/material.dart';

class CardPersonalInfoWidget extends StatelessWidget {
  const CardPersonalInfoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Text("Card Personal Info Widget"),
    );
  }
}