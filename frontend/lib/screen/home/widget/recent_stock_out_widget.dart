import 'package:flutter/material.dart';

class RecentStockOutWidget extends StatelessWidget {
  const RecentStockOutWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Recent Stock Out Screen"),
    );
  }
}