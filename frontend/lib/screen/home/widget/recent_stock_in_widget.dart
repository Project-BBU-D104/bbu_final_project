import 'package:flutter/material.dart';

class RecentStockInWidget extends StatelessWidget {
  const RecentStockInWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Recent Stock In Screen"),
    );
  }
}