import 'package:flutter/material.dart';

class PaymentCardSummaryWidget extends StatelessWidget {
  const PaymentCardSummaryWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Text("Payment Card Detail Widget"),
    );
  }
}