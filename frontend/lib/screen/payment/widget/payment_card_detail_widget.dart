import 'package:flutter/material.dart';

class PaymentCardDetailWidget extends StatelessWidget {
  const PaymentCardDetailWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Text("Payment Card Detail Widget"),
    );
  }
}