import 'package:flutter/material.dart';

class SupplierCardWidget extends StatelessWidget {
  const SupplierCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}