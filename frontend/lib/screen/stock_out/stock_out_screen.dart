import 'package:flutter/material.dart';

class StockOutScreen extends StatelessWidget {
  const StockOutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Stock Out Screen")),
    );
  }
}