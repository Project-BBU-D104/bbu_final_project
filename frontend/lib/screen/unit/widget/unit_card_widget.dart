import 'package:flutter/material.dart';

class UnitCardWidget extends StatelessWidget {
  const UnitCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Text("Unit Card Widget"),
    );
  }
}