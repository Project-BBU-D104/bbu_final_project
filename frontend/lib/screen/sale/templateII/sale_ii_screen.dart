import 'package:flutter/material.dart';

class SaleIiScreen extends StatelessWidget {
  const SaleIiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Sale II Screen"),
    );
  }
}