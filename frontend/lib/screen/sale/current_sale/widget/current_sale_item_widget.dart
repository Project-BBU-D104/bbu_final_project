import 'package:flutter/material.dart';

class CurrentSaleItemWidget extends StatelessWidget {
  const CurrentSaleItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Text("Current Sale Item Widget"),
    );
  }
}