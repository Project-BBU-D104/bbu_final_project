import 'package:flutter/material.dart';

class WarehouseStockCardWidget extends StatelessWidget {
  const WarehouseStockCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      
    );
  }
}