import 'package:flutter/material.dart';

class WarehouseStockDetailWidget extends StatelessWidget {
  const WarehouseStockDetailWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}