import 'package:flutter/material.dart';

class AddWarehouseStockWidget extends StatelessWidget {
  const AddWarehouseStockWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}