import 'package:flutter/material.dart';

class StockInAndStockOutScreen extends StatelessWidget {
  const StockInAndStockOutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Stock In & Stock Out Screen")),
    );
  }
}