import 'package:flutter/material.dart';

class StockLedgerWidget extends StatelessWidget {
  const StockLedgerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Stock Ledger Widget"),
    );
  }
}