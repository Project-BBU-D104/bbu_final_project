enum StorageKey {
  workspaces,
  currentLanguage,
  currentWorkspace,
  isBackToWorkspace,
  allowMultipleSite,
  rawCookie,
  userLogon,
  currentPOSProfile,
  systemMode,
}

enum ENV { estcServerUrl, aesIv, aesKey }

enum SystemMode { system, light, dark }
